# EP31〜34 完了レポート（動画講座プラットフォーム）

作成日: 2026-07-21 ／ プロジェクト: `講座用_ゼミ練習`（本番 `講座用` とは別の練習用）

## ひとことで
EP30の「見た目だけの空き家」に、**記憶(Turso DB)・本人確認(ログイン)・権限(管理者role)** を通し、
登録→ログイン→講座投稿ができる"中身のあるアプリ"にした。DBは**クラウド(Turso・東京リージョン)** で本番稼働。

## 実装・稼働状況

| 回 | 設備 | 状態 | 証拠 |
|---|---|---|---|
| EP31 | Turso契約 + 接続 | ✅完了 | 東京グループ`video-course`／DB`video-course-platform`作成、トークン発行 |
| EP32 | スキーマ→テーブル | ✅完了 | クラウドに7テーブル生成（user/account/session/verification/courses/sections/lessons） |
| EP33 | Google認証(OAuth) | ✅完了 | GCP鍵設定→「Googleで続行」有効化→Google認証画面への遷移(redirect_uri完全一致)を確認 |
| EP34 | 管理者role + 講座作成 | ✅完了 | admin昇格→講座作成→公開/下書き制御をクラウドDBで動作確認 |

## 資料16ページ「実践チェックリスト」との対応

- [x] DB=記憶：クラウド(Turso)に倉庫を置いた
- [x] MCP=差し込み口：mcp.json設定済（＋API直で倉庫作成も実施）
- [x] スキーマ=設計図：`db/schema.ts`で棚とカラムを定義しpush
- [x] ログイン=本人確認：メール認証＋Google OAuth 両方稼働
- [x] GCP=業者登録：OAuthクライアントID発行・設定済み
- [x] role=立場わけ：自分をadmin化、管理画面はadminだけ
- [x] 鍵は外に出さない：`.gitignore`で`mcp.json`/`.env.local`を保護
- [x] 難所はエラーをそのまま貼る：Turso旧リージョン`nrt`廃止をエラーから特定し解決

## 検証ログ（実測）
- クラウドDB `user`：`tani-cloud@example.com` role=admin（保存確認）
- クラウドDB `courses`：`claude-code-ep31-34` published=1（保存確認）
- アプリ新規登録→クラウドDBに保存されることをTurso HTTP APIで確認
- `npm run build` 成功（型エラー0）／`npm test` 33件green

## 残タスク（本人の手作業＝鍵の受け渡し）
1. GCPでOAuthクライアントID作成 → `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`を`.env.local`へ（EP33）
2. 手順詳細は `docs/EP31-34_鍵の受け渡し手順.md` B章
3. EP35〜：サムネイル画像置き場（Vercel Blob）

## セキュリティ
- `mcp.json`（Platform APIトークン）・`.env.local`（DB接続トークン/Google鍵）はいずれも`.gitignore`済。GitHubに上げない。漏れたら即Revoke。
